# .NET Commands

Common `dotnet` commands for web projects.

```bash
dotnet --info
dotnet new webapi -n MyApi
dotnet restore
dotnet build
dotnet test
dotnet run
```



> [!TIP]
> Use the **Copy** button in the top-right of code blocks to quickly copy commands to your clipboard.
