# Install Firebase

Install Firebase SDK + Firebase CLI.

## Web SDK

```bash
npm i firebase
```

## Firebase CLI

```bash
npm i -g firebase-tools
firebase --version
```



> [!TIP]
> Use the **Copy** button in the top-right of code blocks to quickly copy commands to your clipboard.
